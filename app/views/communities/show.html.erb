<%= render 'shared/community_cover', community: @community %>
<br>
<div id="user-<%= @community.id %>" class="row">
  <div class="col-lg-3">
    <%= render 'shared/community_info' %>
  </div>

  <div class="col-lg-6">
    <div id="activities">

    </div>
  </div>
  <div class="col-lg-3">
    <% if is_current_user?(@user) %>
      <div class="well">
        <ul class="nav nav-pills nav-stacked">
          <li><%= link_to 'Edit Community', edit_community_path(@community) %></li>
          <li><%= link_to 'Followers', followers_community_path(@community) %></li>
          <li><%= link_to 'Deactivate', deactivate_community_path(@community) %></li>
        </ul>
      </div>
    <% end %>
  </div>
</div>
<!-- <p id="notice"><%= notice %></p>


<p>
  <strong>Name:</strong>
  <%= @community.name %>
</p>

<p>
  <strong>About:</strong>
  <%= @community.about %>
</p>

<p>
  <%= @community.user_id %>
</p>

<p>
  <strong>Avatar:</strong>
  <%= @community.avatar %>
</p>

<p>
  <strong>Cover:</strong>
  <%= @community.cover %>
</p>

<p>
  <strong>Slug:</strong>
  <%= @community.slug %>
</p>

<p>
  <strong>Posts count:</strong>
  <%= @community.posts_count %>
</p>

<%= link_to 'Edit', edit_community_path(@community) %> |
<%= link_to 'Back', communities_path %> -->
